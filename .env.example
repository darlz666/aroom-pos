# Copy to .env and replace the placeholders with local PostgreSQL credentials.
# Server-side only. Never commit real credentials.
DATABASE_URL="postgresql://aroom:CHANGE_ME@localhost:5432/aroom_pos?schema=public"
